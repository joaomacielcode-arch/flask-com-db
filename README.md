# flask-com-db
Pequeno Projeto sobre API

imports:

from sqlalchemy import create_engine, Column, String, Integer, Boolean, Float, ForeignKey
from fastapi import FastAPI

Pra funcionar só baixar o banco de dados "dtbank.db"
